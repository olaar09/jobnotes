import {FormattedMessage} from 'react-intl';
import React from 'react';
export interface WithFortmattedText {
  text: any;
}

export interface WithFortmattedTitle {
  title: any;
}

export interface WithFortmattedName {
  name: any;
}

export interface WithFortmattedPlaceHolder {
  placeholder: Element;
}
